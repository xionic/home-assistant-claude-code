{
  "name": "claude-code-ui-dev",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Development/test manifest for the Claude Code UI app. NOT shipped in the image — server/package.json is the runtime manifest.",
  "scripts": {
    "test": "NODE_PATH= node --test test/unit/ test/integration/ test/cli/",
    "test:unit": "NODE_PATH= node --test test/unit/",
    "test:server": "NODE_PATH= node --test test/integration/",
    "test:cli": "NODE_PATH= node --test test/cli/",
    "test:browser": "NODE_PATH= node --test test/browser/",
    "test:live": "NODE_PATH= node test/live/run.mjs"
  },
  "//scripts": "NODE_PATH is cleared because some hosts export a global one, which would otherwise satisfy imports from outside this project and hide a missing dependency.",
  "devDependencies": {
    "express": "^4.21.0",
    "puppeteer-core": "^24.0.0",
    "ws": "^8.18.0"
  }
}
